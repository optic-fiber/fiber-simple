package com.cheroliv.fiber.config

import groovy.transform.NotYetImplemented

class ApplicationUtils {

    //TODO
    @NotYetImplemented
    static void secureRequestedPath(String path) {
        //1)tokenize path
        //2)ensure every token from user.home to the endpoint
        //path is valid,
        //3)otherwise put the system in a secure state
        //4)finish the method with assertions

    }
}
